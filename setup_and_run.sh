#!/bin/bash

echo "🚀 جاري تجهيز بيئة العمل لمشروع TTS Spark..."

# 1. تحديث النظام وتنصيب المتطلبات الأساسية
sudo apt-get update
sudo apt-get install -y python3-pip python3-dev

# 2. تنصيب المكتبات البرمجية
echo "📦 جاري تنصيب المكتبات..."
pip3 install -r requirements.txt

# 3. التأكد من وجود المجلدات الضرورية
if [ ! -d "espeak-ng-data" ]; then
    echo "⚠️ تحذير: مجلد espeak-ng-data غير موجود! تأكد من نقله ليعمل الصوت بشكل صحيح."
fi

# 4. تشغيل السيرفر
echo "✅ كل شيء جاهز. جاري تشغيل الخادم..."
python3 spark_server.py
